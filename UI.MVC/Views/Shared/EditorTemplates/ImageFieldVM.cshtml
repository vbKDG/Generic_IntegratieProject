@model D.UI.MVC.Models.Fields.ImageFieldVm
<link rel="stylesheet" href="~/css/carousel.css"> 

<div>
    <label>Image:</label>
    @*   <input id="image" name="image" type="file"/> #pogin 1
    @Html.TextBoxFor(m => m.file, new {type = "file"})#poging 2 
    <input type="file" id="imageFile" name="imageFile" value="ActionHandlerForForm"/> #poging3          *@
    <input asp-for="imageName" type="hidden" class="form-control" value="test"/>
    <input asp-for="imageFile" type="file" 
           class="form-control"
           onchange="readImage(this)" 
           multiple
           accept="image/*">
    <img id="image_preview" src="#" alt="your image" width="200" height="200" hidden=""/>
    
    <p> pppppppppppppppp </p>
    
    <div class="slideshow-container">
        
        <div class="mySlides">
            <img id="carousel1" src="#" width="200" height="200"/>
        </div>
        
        <!-- Next and previous buttons -->
        <a class="prev" onclick="plusSlides(-1)">&#10094;</a>
        <a class="next" onclick="plusSlides(1)">&#10095;</a>
        
    </div>
    

        
       
 

    <!-- The dots/circles -->
    <div style="text-align:center">
        <span class="dot" onclick="currentSlide(1)"></span> 
        <span class="dot" onclick="currentSlide(2)"></span> 
        <span class="dot" onclick="currentSlide(3)"></span> 
    </div>
    
    
    @*<input asp-for="imageFiles" type="file" 
           class="form-control"
           onchange="readImage(this)" 
           hidden="true"
           multiple
           accept="image/*">*@


    
</div>
<script>
    var slideIndex = 1;
    showSlides(slideIndex);

// Next/previous controls
    function plusSlides(n) {
        showSlides(slideIndex += n);
    }

// Thumbnail image controls
    function currentSlide(n) {
        showSlides(slideIndex = n);
    }

    function showSlides(n) {
        var i;
        var slides = document.getElementsByClassName("mySlides");
        var dots = document.getElementsByClassName("dot");
        if (n > slides.length) {slideIndex = 1} 
        if (n < 1) {slideIndex = slides.length}
        for (i = 0; i < slides.length; i++) {
            slides[i].style.display = "none"; 
        }
        for (i = 0; i < dots.length; i++) {
            dots[i].className = dots[i].className.replace(" active", "");
        }
        slides[slideIndex-1].style.display = "block"; 
        dots[slideIndex-1].className += " active";
    }
</script>
<script type="text/javascript">
    var inputString = "";
    function readImage(input) {
        
        for(var  i = 0; i < input.files.length; i++)
        {
            inputString += '<img id="carousel' + i + '" src="#" width="200" height="200"/>';
            $(".slideshow-container").append('<div class="mySlides">'+ inputString +'</div>');
        }              
        
      //  inputString += '<img id="carousel2" src="#" width="200" height="200"/>';
        
//        <div class="mySlides fade">
//            <div class="numbertext">1 / 3</div>
//            <img src="img1.jpg" style="width:100%">
//            <div class="text">Caption Text</div>
//            </div>

        // inputstring =+ '<img id="carousel2" src="#" width="200" height="200"/>';
     

      //  $(".slideshow-container").append('<div class="mySlides">'+ inputString +'</div>');
        for(var i = 0; i < input.files.length; i++) {
            (function(file) {
                var reader1 = new FileReader();
                //var name = '#carousel' + i;
                reader1.onload = function(e) {
                    $('#carousel1').attr('src', e.target.result);
                    
                }
                reader1.readAsDataURL(input.files[i]);

            })(input.files[i]);
            
            
            
            
          

        }   
     /*
   
        if (input.files && input.files[0]) {
            var reader = new FileReader();

            reader.onload = function(e) {
                $('#image_preview').attr('src', e.target.result);
              //  $('#carousel2').attr('src', e.target.result);

                $('#image_preview').attr('hidden', false);
                
                
                


            }

            reader.readAsDataURL(input.files[0]);
            // $('#imageFieldVM_imageFiles').attr('hidden', false);


          
            // $('input[name="imageFieldVM.imageFile"]').style.display='none';
           

        }*/
    }
</script>


@*
@model D.UI.MVC.Models.Ideas.ImageFieldViewModel
<div>
    <label>Image:</label>
    $1$   <input id="image" name="image" type="file"/> #pogin 1
    @Html.TextBoxFor(m => m.file, new {type = "file"})#poging 2 
    <input type="file" id="imageFile" name="imageFile" value="ActionHandlerForForm"/> #poging3          #1#
    <input asp-for="imageName" type="hidden" class="form-control" value="test"/>
    <input asp-for="imageFile" type="file" class="form-control"  multiple  >
    

    
</div>*@
